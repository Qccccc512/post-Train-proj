# Colab runtime lockfile for Qwen/Qwen3-8B text-only LoRA SFT with Unsloth.
#
# Version choices are based on:
# - `colab_info.txt` (Google Colab runtime package baseline on 2026-03-25)
# - Official Qwen/Qwen3-8B model card + Hugging Face Qwen3 docs
# - Official Unsloth install docs + Qwen3 fine-tuning guide
#
# Notes:
# - This lockfile prefers a stack close to the current Colab baseline while
#   keeping newer acceleration packages that are compatible with Qwen3 +
#   Unsloth.
# - We intentionally keep the Colab-near core stack:
#   - `torch==2.10.0`
#   - `torchvision==0.25.0`
# - And pair it with newer compatible acceleration packages:
#   - `xformers==0.0.35`
#   - `flash-linear-attention==0.4.1`
#   - `causal_conv1d==1.6.1`
# - `unsloth` and `unsloth_zoo` remain pinned to the same release family for
#   reproducibility and to avoid resolver conflicts.

torch==2.10.0
transformers==5.2.0
accelerate==1.13.0
peft==0.18.1
datasets==4.3.0
huggingface_hub==1.7.1
unsloth==2026.3.4
unsloth_zoo==2026.3.4
trl==0.22.2
tokenizers==0.22.2
bitsandbytes==0.48.1
torchvision==0.25.0
xformers==0.0.35
flash-linear-attention==0.4.1
causal_conv1d==1.6.1
Pillow==11.3.0
PyYAML==6.0.3
safetensors==0.7.0
